const { resolve } = require( "path" );

module.exports = {
   config: resolve( __dirname, "src", "config","database",'postgres', "index.js" ),
    "models-path": resolve( "./src/models" ),
    "seeders-path": resolve( "./src/seeders" ),
    "migrations-path": resolve( "./src/migrations" )
};